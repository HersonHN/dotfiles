#!/usr/bin/env bash

set -e

FULL_PATH="$1"
DIRECTORY="$(dirname $FULL_PATH)"
FILE="$(basename $FULL_PATH)"
DECODED_FILE="decoded_${FILE/.gpg/}"

cd "$DIRECTORY"
gpg -d -o "$DECODED_FILE" "$FILE"
echo "\"$FILE\" decoded to \"$DECODED_FILE\""
